import AddPost from 'features/add-post/ui/add-post';
import { FC } from 'react';

const ClientPage: FC = () => {
    return (
        <div>
            <AddPost />
        </div>
    );
};

export default ClientPage;